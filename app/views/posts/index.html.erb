<h2> See Who is Vactioning!</h2>

<div class="imagebackground">

<% @posts.each do |post| %>
		
	<%= image_tag post.image.url(:medium) %>
	<br>
	<%= post.description %>
	<br>
	<%= link_to 'Destroy', post_path(post.id),
	              method: :delete,
	              data: { confirm: 'Are you sure?' } %>

	<br>
</div>

<% end %>

<%= link_to 'New Post', new_post_path %>